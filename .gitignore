node_modules
.next
out
.env*.local
*.tsbuildinfo
next-env.d.ts

# The runtime JSON store at the repo root, regenerated from
# src/lib/data/seed.ts on first run. Anchored with a leading slash: a bare
# "data/" also matches src/lib/data/, which is source.
/data/
